# Commands

No package manager or build scripts are detected for this docs repo.

Use:

- `git status --short`
- `rg -n "TODO|FIXME|secret|token|password|private" README.md docs *.md`
- Manual link/path review for docs-only changes.
